{% extends 'base.html.twig' %}

{% block title %}Plat{% endblock %}

{% block body %}
<div class="container my-5">
    <h1>Plat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plat.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ plat.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plat_index', {'resto': resto}) }}">back to list</a>

    <a href="{{ path('plat_edit', {'id': plat.id, 'resto': resto}) }}">edit</a>

    {{ include('plat/_delete_form.html.twig', {'resto': resto}) }}
    </div>
{% endblock %}
